# 2026-09-12 共用选股任务验收

**本报告的验收通过结论已撤回。** 用户随后指出：周六不应自动改成周五来出票。
并明确要求直接请求 realtime，不新增周末/节假日判断；空数据不出票。
下文保留历史执行事实，仅证明旧任务曾输出并送达周五结果，不能证明用户要求的逻辑一致。

## 错误与预防

本次可定位的根因：把助手自行增加的“盘后手工只读核查”当成用户需求，进一步写进
文档和测试；随后用该错误标准证明“成功”。只验证计算函数相同或核查消息送达，
无法证明用户要求的完整任务相同。

用户明确要求已写入 AGENTS.md 和 execution-contract.md，并优先于旧文档及旧测试。
先新增行为回归检查，再修实现。修复前四项检查均失败，证明能抓住原问题：
[失败的 CI](https://github.com/WidgetA/A-share-quant-trading/actions/runs/34626029077)。
镜像构建依赖独立的 Selection Task Contract 检查及真实 PostgreSQL 检查。

## 已验证的实现

[PR #7](https://github.com/WidgetA/A-share-quant-trading/pull/7) 已合并。
生产代码提交：`d33935135ad0c93435b22156e3a724ea81c04ee6`。
[该提交的 CI](https://github.com/WidgetA/A-share-quant-trading/actions/runs/34628850576)
全部通过：1,681 项单元测试、94 项 PostgreSQL 测试；另有 1 项跳过、2 项取消选择，
不计入通过数量。

- 定时器和按钮进入同一完整任务；已删除盘后核查分流的 HTTP 路由逻辑。
- 新请求重新计算，保存普通选股结果，使用同一消息格式和生产投递器。
- 每日状态只推进一次；同编号并发重试不重复执行或发送。
- 真实数据库验证 V22 放行与阻断、两种首次触发、保存回滚、并发 HTTP 请求、
  正常投递及回执丢失；外部行情使用受控事实，取数边界与真实计算入口由专门测试覆盖。
- 卖出提醒用股票、推荐日期、全部卖出的范围和生效时间说明操作，去掉“模型腿”等表述。

## 实际生产按钮运行

先由 `/api/status` 确认上述提交已部署且服务健康，再通过原
`POST /api/v20/trigger-scan` 运行，没有额外测试或核查入口。

- 请求编号：`selection-task-20260912-d339351-001`。
- 目标交易日：`2026-09-11`（执行时为 9 月 12 日，使用最近一个交易日）。
- 新运行：`created=true`，计算完成并保存结果。
- 结果事件：`60b271674d2f444ac9c5017552fdeaf79a285e20b2ce56d06ab66cdddd8f8208`。
- 策略：`V22-slim`，结果 `BLOCK`，新开仓比例 0，保存完整 Top10 参考名单。
- 同编号后续查询：`created=false`，事件不变，每日状态未再次推进。
- 最终：`delivery_status=SENT`、`feishu_delivery_confirmed=true`、`task_success=true`。

投递过程中曾读到 `PENDING`、`DELIVERY_UNKNOWN`，当时未判定成功。
投递器在请求进行中也先标记不确定，以避免重复发送；收到回执后才变为 `SENT`。
同编号查询最终确认送达，没有为追求成功另造一条消息或改写原投递状态。

这份证据验证本次部署的共用任务与实际投递；后续版本需要自己的验收记录。
